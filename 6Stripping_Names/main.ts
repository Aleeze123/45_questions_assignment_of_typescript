// Store a person’s name with some whitespace characters at the beginning and end of the name
let whitespaceName = "\n\t Aleeze Batool \t\n";

// Print the name once, so the whitespace around the name is displayed. 

console.log(whitespaceName);

// Store a person's name without whitespace
let withoutWhitespaceName = whitespaceName.trim();

// print the name without whitespace
console.log(withoutWhitespaceName);




